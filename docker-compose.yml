name: inv_ibkr
# One file, two IB applications. `IB_APP` in .env picks which one, by feeding
# COMPOSE_PROFILES; `bastion` carries no profile, so it always starts.
# See "Choosing the application" in README.md.
services:
  ib-gateway:
    profiles:
      - ib-gateway
    restart: always
    container_name: ${CONTAINER_NAME:-ib-gateway}
    # Everything here runs as uid 1000 - Xvfb, x11vnc, socat, ssh, the JVM -
    # and none of it needs a capability. Verified 2026-08-30 by starting all
    # four under `--cap-drop ALL --security-opt no-new-privileges:true`.
    # no-new-privileges also neuters the passwordless-root grant that is still
    # baked into the published image (sudo refuses: "the no new privileges flag
    # is set"), so it mitigates that today rather than at the next republish.
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    pids_limit: 512
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"
    build:
      context: ./latest
      tags:
        - "ghcr.io/dennisdeh/ib-gateway:latest"
    pull_policy: build
    image: ghcr.io/dennisdeh/ib-gateway:latest
    environment:
      TWS_USERID: ${TWS_USERID}
      TWS_PASSWORD: ${TWS_PASSWORD}
      TRADING_MODE: ${TRADING_MODE:-paper}
      TWS_USERID_PAPER: ${TWS_USERID_PAPER:-}
      TWS_PASSWORD_PAPER: ${TWS_PASSWORD_PAPER:-}
      TWS_SETTINGS_PATH: ${TWS_SETTINGS_PATH:-}
      TWS_ACCEPT_INCOMING: ${TWS_ACCEPT_INCOMING:-manual}
      TWS_MASTER_CLIENT_ID: ${TWS_MASTER_CLIENT_ID:-}
      READ_ONLY_API: ${READ_ONLY_API:-}
      VNC_SERVER_PASSWORD: ${VNC_SERVER_PASSWORD:-}
      TWOFA_TIMEOUT_ACTION: ${TWOFA_TIMEOUT_ACTION:-exit}
      BYPASS_WARNING: ${BYPASS_WARNING:-}
      AUTO_RESTART_TIME: ${AUTO_RESTART_TIME:-}
      AUTO_LOGOFF_TIME: ${AUTO_LOGOFF_TIME:-}
      TWS_COLD_RESTART: ${TWS_COLD_RESTART:-}
      SAVE_TWS_SETTINGS: ${SAVE_TWS_SETTINGS:-}
      RELOGIN_AFTER_TWOFA_TIMEOUT: ${RELOGIN_AFTER_TWOFA_TIMEOUT:-no}
      TWOFA_EXIT_INTERVAL: ${TWOFA_EXIT_INTERVAL:-60}
      TWOFA_DEVICE: ${TWOFA_DEVICE:-}
      EXISTING_SESSION_DETECTED_ACTION: ${EXISTING_SESSION_DETECTED_ACTION:-primary}
      ALLOW_BLIND_TRADING: ${ALLOW_BLIND_TRADING:-no}
      TIME_ZONE: ${TIME_ZONE:-Etc/UTC}
      TZ: ${TIME_ZONE:-Etc/UTC}
      CUSTOM_CONFIG: ${CUSTOM_CONFIG:-NO}
      JAVA_HEAP_SIZE: ${JAVA_HEAP_SIZE:-}
      SSH_TUNNEL: ${SSH_TUNNEL:-}
      SSH_OPTIONS: ${SSH_OPTIONS:-}
      SSH_ALIVE_INTERVAL: ${SSH_ALIVE_INTERVAL:-}
      SSH_ALIVE_COUNT: ${SSH_ALIVE_COUNT:-}
      SSH_PASSPHRASE: ${SSH_PASSPHRASE:-}
      SSH_REMOTE_PORT: ${SSH_REMOTE_PORT:-}
      SSH_USER_TUNNEL: ${SSH_USER_TUNNEL:-}
      SSH_RESTART: ${SSH_RESTART:-}
      SSH_VNC_PORT: ${SSH_VNC_PORT:-}
      START_SCRIPTS: ${START_SCRIPTS:-}
      X_SCRIPTS: ${X_SCRIPTS:-}
      IBC_SCRIPTS: ${IBC_SCRIPTS:-}
    volumes:
        - ./ssh/:/home/ibgateway/.ssh
  #      - ./jts.ini:/home/ibgateway/Jts/jts.ini
  #      - ./config.ini:/home/ibgateway/ibc/config.ini
  #      - ./tws_settings/:${TWS_SETTINGS_PATH:-/home/ibgateway/tws_settings}
  #      - ./scripts:/home/ibgateway/init-scripts
    ports:
      - "127.0.0.1:${PORT_HOST_TWS_LIVE:-4001}:4003"
      - "127.0.0.1:${PORT_HOST_TWS_PAPER:-4002}:4004"
      - "127.0.0.1:${PORT_HOST_VNC_SERVER:-5900}:5900"

  tws:
    profiles:
      - tws
    restart: unless-stopped
    container_name: ${CONTAINER_NAME_TWS:-tws}
    devices:
      - /dev/dri:/dev/dri #recomended
    shm_size: "1gb" #recomended
    security_opt:
      - seccomp:unconfined #recomended
    build:
      context: ./latest
      dockerfile: Dockerfile.tws
      args:
        # tws builds FROM the gateway image. Defaulting to the tag the
        # ib-gateway service above produces means `docker compose build
        # ib-gateway` then `docker compose build tws` verifies the pair built
        # here; ghcr.io answers anonymously, so without this the base would
        # come from the registry instead. See docs/DECISIONS.md #30.
        IB_GATEWAY_BASE_IMAGE: ${IB_GATEWAY_BASE_IMAGE:-ghcr.io/dennisdeh/ib-gateway}
        IB_VERSION: ${IB_GATEWAY_TAG:-latest}
      tags:
        - "ghcr.io/dennisdeh/tws-rdesktop:latest"
    pull_policy: build
    image: ghcr.io/dennisdeh/tws-rdesktop:latest
    environment:
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
      PASSWD: ${PASSWD:-}
      TWS_USERID: ${TWS_USERID}
      TWS_PASSWORD: ${TWS_PASSWORD}
      TRADING_MODE: ${TRADING_MODE:-paper}
      TWS_USERID_PAPER: ${TWS_USERID_PAPER:-}
      TWS_PASSWORD_PAPER: ${TWS_PASSWORD_PAPER:-}
      TWS_SETTINGS_PATH: ${TWS_SETTINGS_PATH:-}
      TWS_ACCEPT_INCOMING: ${TWS_ACCEPT_INCOMING:-manual}
      TWS_MASTER_CLIENT_ID: ${TWS_MASTER_CLIENT_ID:-}
      READ_ONLY_API: ${READ_ONLY_API:-}
      TWOFA_TIMEOUT_ACTION: ${TWOFA_TIMEOUT_ACTION:-exit}
      BYPASS_WARNING: ${BYPASS_WARNING:-}
      AUTO_RESTART_TIME: ${AUTO_RESTART_TIME:-}
      AUTO_LOGOFF_TIME: ${AUTO_LOGOFF_TIME:-}
      TWS_COLD_RESTART: ${TWS_COLD_RESTART:-}
      SAVE_TWS_SETTINGS: ${SAVE_TWS_SETTINGS:-}
      RELOGIN_AFTER_TWOFA_TIMEOUT: ${RELOGIN_AFTER_TWOFA_TIMEOUT:-no}
      TWOFA_EXIT_INTERVAL: ${TWOFA_EXIT_INTERVAL:-60}
      TWOFA_DEVICE: ${TWOFA_DEVICE:-}
      EXISTING_SESSION_DETECTED_ACTION: ${EXISTING_SESSION_DETECTED_ACTION:-primary}
      ALLOW_BLIND_TRADING: ${ALLOW_BLIND_TRADING:-no}
      TIME_ZONE: ${TIME_ZONE:-Etc/UTC}
      TZ: ${TIME_ZONE:-Etc/UTC}
      CUSTOM_CONFIG: ${CUSTOM_CONFIG:-NO}
      JAVA_HEAP_SIZE: ${JAVA_HEAP_SIZE:-}
      SSH_TUNNEL: ${SSH_TUNNEL:-}
      SSH_OPTIONS: ${SSH_OPTIONS:-}
      SSH_ALIVE_INTERVAL: ${SSH_ALIVE_INTERVAL:-}
      SSH_ALIVE_COUNT: ${SSH_ALIVE_COUNT:-}
      SSH_PASSPHRASE: ${SSH_PASSPHRASE:-}
      SSH_REMOTE_PORT: ${SSH_REMOTE_PORT:-}
      SSH_USER_TUNNEL: ${SSH_USER_TUNNEL:-}
      SSH_RESTART: ${SSH_RESTART:-}
      SSH_RDP_PORT: ${SSH_RDP_PORT:-}
      START_SCRIPTS: ${START_SCRIPTS:-}
      X_SCRIPTS: ${X_SCRIPTS:-}
      IBC_SCRIPTS: ${IBC_SCRIPTS:-}
    volumes:
      - ./config:/config # optional
      - tws_tmp:/tmp # recommended for performance
    # custom certificate and key
      - ./keylock:/keylock # required to avoid key generation by container
      - ./key.pem:/etc/xrdp/key.pem # key
      - ./cert.pem:/etc/xrdp/cert.pem # cert
    ports:
      - "127.0.0.1:${PORT_HOST_RDESKTOP_LIVE:-7496}:7498"
      - "127.0.0.1:${PORT_HOST_RDESKTOP_PAPER:-7497}:7499"
      - "127.0.0.1:${PORT_HOST_RDP:-3370}:3389"

  bastion:
    build:
      context: ./bastion
      platforms:
        - "linux/amd64"
        - "linux/arm64"
      args:
        APT_PROXY: ${APT_PROXY}
        BASE_VERSION: ${BASE_VERSION}
        # Without this the image's version label reads "-resolute".
        IMAGE_VERSION: ${IMAGE_VERSION}
      tags:
        - "ghcr.io/dennisdeh/bastion:latest"
    restart: unless-stopped
    # Published by publish.yml alongside the other two, so a deployment can
    # pull it; this file still builds it, which is how it is developed - and
    # `pull_policy: build` keeps it from pulling the published ghcr.io tag over
    # the image just built here, exactly as the two services above do.
    pull_policy: build
    image: ghcr.io/dennisdeh/bastion:latest
    container_name: ${CONTAINER_NAME_BASTION:-bastion}
    # sshd runs as root and needs six of the fourteen default capabilities.
    # Measured 2026-08-30 against this image: `cap_drop: [ALL]` alone breaks
    # authentication outright, and this set restores key auth and -L forwarding.
    # Do not trim it by inspection - re-measure.
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
      - SETGID
      - SETUID
      - SYS_CHROOT
      - AUDIT_WRITE
    security_opt:
      - no-new-privileges:true
    # This is the one service reachable from off the host, and it forks per
    # connection. `MaxStartups 10:30:100` bounds sshd's own accept rate; these
    # bound what the container can do to the host if that is ever not enough.
    pids_limit: 256
    mem_limit: 256m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"
    ports:
      - ${SSH_LISTEN_PORT}:22
    environment:
      # bastion/entrypoint.sh reads all of these at runtime. Passing only USERS
      # meant TOTP_ENABLED, CA_ENABLED and BANNER_ENABLED set in .env were
      # silently ignored when starting from the repository root.
      USERS: ${BASTION_USERS:-ibgateway}
      USER_SHELL: ${USER_SHELL:-}
      TOTP_ENABLED: ${TOTP_ENABLED:-no}
      TOTP_ISSUER: ${TOTP_ISSUER:-Bastion}
      TOTP_QR_ENCODE: ${TOTP_QR_ENCODE:-UTF8}
      CA_ENABLED: ${CA_ENABLED:-no}
      SSHD_HOST_CERT: ${SSHD_HOST_CERT:-}
      SSHD_USER_CA: ${SSHD_USER_CA:-}
      BANNER_ENABLED: ${BANNER_ENABLED:-no}
    volumes:
      - ./data/etc/passwd:/etc/passwd:ro
      - ./data/etc/shadow:/etc/shadow:ro
      - ./data/etc/group:/etc/group:ro
      - ./data/etc/ssh:/etc/ssh:ro
      - ./data/home:/home:ro

volumes:
  tws_tmp:
